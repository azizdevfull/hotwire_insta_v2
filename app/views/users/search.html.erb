<% @users.each do |user| %>
  <li class="list-group-item" role="option" data-autocomplete-value="<%= user.id %>" data-autocomplete-label="<%= user.username %>">
    <%= link_to user.username, user %>
    <%# <span class="badge"><%= bird.description</span> %>
  </li>
<% end %>